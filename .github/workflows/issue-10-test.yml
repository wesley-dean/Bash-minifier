name: Issue 10 verification

on:
  push:
    branches:
      - fix/issue-10-trailing-backslashes

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run test suite
        run: bash test/test.sh
